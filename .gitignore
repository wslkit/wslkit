*.exe
*.test
*.out
dist/
spikes/**/*.exe
testdata/fuzz/
.idea/
.vscode/
internal/agent/payload/files/wslkit-agent-linux-*

# The documentation site is generated from docs/ and from the data tables the
# binary embeds. Never edit it; edit docs/, or the data file behind the page.
site/content/
site/public/
site/.hugo_build.lock

# A machine capture belongs in testdata/snapshots as a named fixture, not
# loose in the repository root. One got committed by a stray `git add -A`.
/*.json
